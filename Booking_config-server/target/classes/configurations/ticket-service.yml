spring:
  datasource:
    url: jdbc:mysql://${MYSQL_URL:localhost:3306}/spms_ticket_db
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:Ijse@123}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
